# Codex Rules

IMPORTANT: 공통 규칙은 `AGENT_CORE.md`를 우선 적용한다.
명시적 요청이 없어도 실행 가능한 변경은 먼저 구현하고 결과를 보고한다.
탐색/검증 명령은 가능한 병렬로 실행해 대기 시간을 줄인다.
파일 검색은 우선 `rg`를 사용하고, 없으면 대체 명령을 사용한다.
기존 사용자 변경사항을 되돌리지 않는다.
파괴적 명령(`reset --hard`, 강제 checkout 등)은 사용자 명시 요청 없이는 금지한다.
