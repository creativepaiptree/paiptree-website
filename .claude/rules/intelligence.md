---
paths:
  - "src/lib/**"
  - "src/app/**"
  - ".env*"
  - "package.json"
---

# Intelligence Rules

외부 API 연동 코드는 타임아웃/에러 경로를 먼저 구현하고 성공 경로를 붙인다.
환경변수 키는 하드코딩하지 말고 누락 시 실패 메시지를 명시한다.
외부 의존성 추가 전 대체 가능한 기존 의존성 사용 여부를 먼저 확인한다.
의존성 추가/업데이트 시 이유 1줄과 영향 범위를 변경 보고에 포함한다.
민감 정보는 로그/예제 코드/문서에 출력하지 않는다.
